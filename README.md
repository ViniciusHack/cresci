# Vinícius Hack

Site pessoal: ponto de vista, produtos e um convite para conversar.

## Como rodar

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — ambiente local
- `npm run build` — build de produção
- `npm run deploy` — build e publicação no Worker
